import React from 'react';
import { Container, Typography } from '@mui/material';

function ResumeBuilder() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Resume Builder
      </Typography>
      {/* Resume Builder content goes here */}
    </Container>
  );
}

export default ResumeBuilder;
